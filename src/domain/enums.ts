export enum ERRORS {
  INTERNAL_SERVER_ERROR = 'Internal Server Error',
  MULTIPLE_REGISTRATIONS = 'Multiple registrations found',
}

export enum MESSAGES {
  NEW_CERTIFICATE_RECEIVED = 'New certificate received.',
}
